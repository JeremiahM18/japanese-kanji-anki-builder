# v0.2.0 Scoped Obsidian Package Lock

Date: 2026-06-10

This lock records the scoped package state for `0.2.0`. The authoritative source is the merged commit that contains this file. Do not create the official `v0.2.0` release tag until release artifact QA closes `PROD-REL-001` and release-trust gates pass.

## Scope

Included:

- Word deck: N5 and N4.
- Core kanji deck: N5, N4, N3, and N2.

Not included:

- N3, N2, or N1 word certification.
- N1 core kanji certification.
- Additional-unverified kanji decks.
- A full release-ready product claim before release artifact QA evidence is complete.
- Human-reviewed provenance. Current Obsidian is non-human governed native/fluent-quality proof; future human/native review is separate provenance for the same standard.
- N2 tracked-source kanji TSV source-depth certification. This lock is for the generated APKG plus Obsidian-certified content scope.

## Certification Evidence

The scoped certification gates were run from the release branch before packaging.

| Gate | Result |
| --- | --- |
| `npm run deck:kanji:obsidian:certify-status -- --levels=5,4,3,2` | Passing; total `982/982` Obsidian certified, `0` needs Obsidian, `0` blocked/failing |
| `npm run deck:words:obsidian:certify-status -- --levels=5,4` | Passing; total `987/987` Obsidian certified, `0` needs Obsidian, `0` blocked/failing |
| `npm run deck:sapphire:n5` | Passing; `80/80` |
| `npm run deck:sapphire:n3` | Passing; `341/341` |
| `npm run deck:platinum:n5` | Passing; `80/80` |
| `npm run deck:platinum:n3` | Passing; `341/341` |
| `node --test test/sapphireTrackedReviewSets.test.js` | Passing; `6/6` |
| `node --test test/obsidianKanjiCertificationStatusService.test.js` | Passing; `4/4` |
| `node --test test/obsidianWordCertificationStatusService.test.js` | Passing; `4/4` |

## Packaged Artifacts

Artifacts are intentionally generated under ignored `out/` directories and are not committed.

| Artifact | Scope | Build command | Count | Size | SHA-256 |
| --- | --- | --- | ---: | ---: | --- |
| `out/word-build/package/japanese-kanji-builder-words-n5-n4.apkg` | Word N5/N4 | `npm run deck:words:ready -- --levels=5,4` | `987` word notes | `40468710` bytes | `40B84BE339F4B109E1041E3A88B7EAE6F1EAFE83207F20000877E63B4358A0E2` |
| `out/build/package/japanese-kanji-builder-n5-n4-n3-n2.apkg` | Core kanji N5/N4/N3/N2 | `npm run deck:ready -- --levels=5,4,3,2` | `982` kanji cards | `45452653` bytes | `0D9685E398847F45D69649580B5EE27FBB3D6910A1905356F33BEF7D83BF73A1` |

Kanji package media completeness from the build:

- Stroke-order fields: `982/982`.
- Audio fields: `982/982`.
- Managed stroke-order coverage: `100.0%`.
- Animation coverage: `100.0%`.
- Audio coverage: `100.0%`.
- Export fallback issues: `0`.

Word package readiness from the build:

- Canonical word rows: `987`.
- N5 readiness: `ready_with_deferred_variants`.
- N4 readiness: `ready_with_deferred_variants`.
- Word audio review: `287/287` N5 and `700/700` N4 ready.
- Pitch accent review: `287/287` N5 and `700/700` N4 annotated.
- True looping animation coverage: `440/440`.

## Required Follow-Up Before Release-Ready Claims

This lock is a scoped content/package lock. Before calling the product release-ready, complete and validate the release QA evidence packet:

```bash
npm run product:release-qa:evidence
```

Current release-trust classification:

- `npm run security:release-trust:pre` fails because `PROD-REL-001` is still open.
- `npm run security:github-settings:auth` fails only on `artifact_attestation_verification_unproven`, because hosted attestation proof requires a successful tagged release workflow run.
- `SEC-P0-004` and `SEC-REQ-007` are pre-release deferred until the tagged workflow produces hosted attestation evidence.
- Do not convert this scoped package lock into a full release-ready claim or official `v0.2.0` release tag until `PROD-REL-001` is closed by release artifact QA evidence and release trust passes.

Release artifact QA must record APKG import, render/layout review, managed-media provenance, manual Anki import result, mobile sanity checks, accessibility/no-color/zoom findings, listening QA, accepted source-governance posture where applicable, and an explicit empty `knownBlockers` array.

If release QA exposes a content defect, stop distribution, fix the source/card data, reopen the affected Sapphire, Platinum, and Obsidian evidence, rerun the gates, rebuild the APKG, and create a new lock.
