# v0.3.0-beta.1 Failed Release Attempt

`v0.3.0-beta.1` is an immutable failed tag, not a published release and not a supported download.

The tagged Release workflow run `30703373330` failed closed on 2026-08-01 before downloading any release input. The verification job's read-only token could not query the private draft prerelease, so `gh release view v0.3.0-beta.1` returned `release not found`. No bundle was created, no asset was attested, and no prerelease was published.

The tag must never be moved, deleted for reuse, or represented as a release. Its immediate correction advanced to the also-failed [v0.3.0-beta.2](v0.3.0-beta.2-n5-automation-preview.md); beta.3 also failed closed, and the current protected-main candidate is [v0.3.0-beta.4](v0.3.0-beta.4-n5-automation-preview.md).
