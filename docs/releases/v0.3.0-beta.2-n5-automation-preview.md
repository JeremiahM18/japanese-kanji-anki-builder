# v0.3.0-beta.2 Failed N5 Preview Attempt

## Outcome

`v0.3.0-beta.2` is an immutable failed release attempt, not a published
release. The tag resolves to commit
`2c12dc7aa082dd1866a921d39419e3b06f7f92d7` and must never be moved, deleted
for reuse, or represented as a distributable version.

Hosted Release workflow run
[`30704224196`](https://github.com/JeremiahM18/japanese-kanji-anki-builder/actions/runs/30704224196)
successfully:

- verified the tag, commit, private draft, and prerelease boundary
- downloaded exactly the two N5 APKGs and packet
- validated packet version, version/tag/commit binding, hashes, byte sizes, and
  exact asset membership
- inspected both APKG structures
- passed supply-chain, license, branch-protection, security-requirements,
  SDLC-metrics, pre-release-trust, secret, SBOM, advisory, and full-test gates

It then failed closed at `npm run product:readiness:n5`. That local-data
coordinator requires ignored `data/kanji_jlpt_only.json`, which correctly is
not present in a clean hosted checkout. Four child checks reported the missing
file. The Release bundle job was skipped, no provenance or attestations were
created, and the draft was not published.

## Correction Boundary

The correction advances to
[v0.3.0-beta.3](v0.3.0-beta.3-n5-automation-preview.md) through protected
`main`. It adds an explicit tracked-only hosted readiness mode and independently
checks all 80 kanji plus all 588 exact word Golden expectations inside the
downloaded APKGs. It does not fabricate ignored local data or weaken the full
local readiness authority.

The required public label remains:

> AUTOMATION-REVIEWED PREVIEW - HUMAN DEVICE QA NOT PERFORMED

No production/GA, human-approved, device-approved, or accessibility-conformance
claim was made for beta.2.
